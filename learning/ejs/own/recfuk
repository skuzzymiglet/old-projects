if too small
branch for each coin

or on steroids:
    basically
    for each coin:
        branch
        if no null:
            return
        else:
            next meme

if too big null
if = return it
